{
  "name": "arbolit-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site and order backend for Arbolit",
  "scripts": {
    "start": "node server/index.js",
    "dev": "npm --prefix server run dev",
    "install:server": "npm --prefix server install",
    "postinstall": "npm --prefix server install --omit=dev"
  },
  "engines": {
    "node": ">=22"
  }
}
